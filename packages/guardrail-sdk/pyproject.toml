[project]
name = "guardrail-sdk"
version = "0.1.0"
description = "Client SDK that enforces Guardrail policy before an agent's tool call executes."
requires-python = ">=3.12"
dependencies = [
    "guardrail-core",
    "httpx>=0.27",
    "pydantic>=2.9",
]

[project.scripts]
# The MCP proxy runs as a command an MCP client launches, so it needs a console
# entry point rather than a module path.
guardrail-mcp = "guardrail_sdk.mcp:stdio_main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/guardrail_sdk"]

[tool.uv.sources]
guardrail-core = { workspace = true }
